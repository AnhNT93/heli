<%= form_for(@new_heli) do |f| %>
  <% if @new_heli.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_heli.errors.count, "error") %> prohibited this new_heli from being saved:</h2>

      <ul>
      <% @new_heli.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
