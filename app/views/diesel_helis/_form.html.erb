<%= form_for(@diesel_heli) do |f| %>
  <% if @diesel_heli.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diesel_heli.errors.count, "error") %> prohibited this diesel_heli from being saved:</h2>

      <ul>
      <% @diesel_heli.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
